import React, { useEffect, useState } from 'react';
import UserInformations from '../../Components/UserInformations';

const User = () => {
    
    return (
        <section>
            <UserInformations />
        </section>
    );
};

export default User;
